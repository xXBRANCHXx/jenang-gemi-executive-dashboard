document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('[data-partner-stock]');if(!root)return;const endpoint=root.dataset.endpoint||'../api/partner-stock/';const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const q=new URLSearchParams(location.search);const state={data:{orders:[],deposits:[],wallets:[],summary:{}},tab:q.has('deposit')?'deposits':'orders',filter:'all',search:'',selectedId:q.get('deposit')||q.get('order')||'',partner:root.dataset.partnerCode||q.get('partner')||''};
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const money=v=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(v||0));const dt=v=>{if(!v)return 'Not yet';const d=new Date(String(v).includes('T')?v:`${String(v).replace(' ','T')}Z`);return Number.isNaN(d.getTime())?'Not yet':new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',timeZone:'Asia/Jakarta'}).format(d)};const relative=v=>{if(!v)return '';const d=new Date(String(v).includes('T')?v:`${String(v).replace(' ','T')}Z`);const s=Math.max(0,Math.round((Date.now()-d)/1000));return s<60?'now':s<3600?`${Math.floor(s/60)}m ago`:s<86400?`${Math.floor(s/3600)}h ago`:`${Math.floor(s/86400)}d ago`};const svg=n=>`<svg viewBox="0 0 24 24" aria-hidden="true">${({box:'<path d="m7.5 4.27 9 5.15M21 8l-9 5-9-5M12 22V12"/>',truck:'<path d="M10 17h4V5H2v12h3M14 9h4l4 4v4h-3M8 20a3 3 0 1 0 0-6 3 3 0 0 0 0 6ZM16 20a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z"/>',check:'<path d="m6 12 4 4 8-8"/>',clock:'<path d="M12 8v4l3 2"/><circle cx="12" cy="12" r="9"/>',wallet:'<path d="M19 7V4a1 1 0 0 0-1-1H5a2 2 0 0 0 0 4h15a1 1 0 0 1 1 1v10a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5M16 13h2"/>',file:'<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8zM14 2v6h6"/>',copy:'<rect x="8" y="8" width="11" height="12" rx="2"/><path d="M16 8V6a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h2"/>',mail:'<rect x="3" y="5" width="18" height="14" rx="2"/><path d="m3 7 9 6 9-6"/>',phone:'<path d="M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3.1 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2.1 4.2 2 2 0 0 1 4.1 2h3a2 2 0 0 1 2 1.7c.1 1 .4 1.9.7 2.8a2 2 0 0 1-.5 2.1L8.1 9.9a16 16 0 0 0 6 6l1.3-1.3a2 2 0 0 1 2.1-.5c.9.3 1.9.6 2.8.7a2 2 0 0 1 1.7 2.1z"/>',pin:'<path d="M20 10c0 5-8 12-8 12S4 15 4 10a8 8 0 1 1 16 0z"/><circle cx="12" cy="10" r="2"/>',scale:'<path d="M12 3v18M5 7h14M5 7l-3 6h6L5 7ZM19 7l-3 6h6l-3-6ZM7 21h10"/>',upload:'<path d="M12 16V4M7 9l5-5 5 5M5 20h14"/>',x:'<path d="M6 6l12 12M18 6 6 18"/>',external:'<path d="M14 3h7v7M10 14 21 3M21 14v5a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5"/>'}[n]||'')}</svg>`;const toast=m=>{const n=$('[data-toast]');$('span',n).textContent=m;n.hidden=false;clearTimeout(toast.t);toast.t=setTimeout(()=>n.hidden=true,2200)};
const req=async(opts={})=>{const url=opts.url||`${endpoint}?${new URLSearchParams(state.partner?{partner:state.partner}:{})}`;const r=await fetch(url,{method:opts.method||'GET',credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json',...(opts.body instanceof FormData?{}:{'Content-Type':'application/json'})},body:opts.body});const p=await r.json().catch(()=>({}));if(!r.ok||p.ok===false)throw new Error(p.error||`HTTP ${r.status}`);return p};
const orderStatus=o=>{const s=String(o.status).toUpperCase();if(s==='FULFILLED')return['done','Fulfilled'];if(['IS_LISTED','IS_BEING_FULFILLED'].includes(s))return['store',s==='IS_BEING_FULFILLED'?'Picking':'At Store Ops'];if(s==='CANCELLED')return['rejected','Cancelled'];return['attention','Arrange shipment']};const depositStatus=d=>({pending:['attention','Needs review'],investigating:['store','Investigating'],approved:['done','Approved'],rejected:['rejected','Rejected']}[d.status]||['',d.status]);const units=o=>(o.items||[]).reduce((n,i)=>n+Number(i.quantity||0),0);const productText=o=>{const a=o.items||[];const f=a[0]?.sku_label||a[0]?.product||'No products';return a.length>1?`${f} +${a.length-1} more`:f};
const match=(...values)=>values.join(' ').toLowerCase().includes(state.search.toLowerCase());const listItems=()=>{if(state.tab==='orders')return state.data.orders.filter(o=>{const [k]=orderStatus(o);return(state.filter==='all'||k===state.filter||(state.filter==='done'&&k==='rejected'))&&match(o.id,o.partner_name,o.partner_code,o.recipient_name,productText(o),...(o.items||[]).map(i=>i.sku_code))});if(state.tab==='deposits')return state.data.deposits.filter(d=>match(d.partner_name,d.partner_code,d.id,d.proof_name,d.status));return state.data.wallets.filter(w=>match(w.partner_name,w.partner_code));};
const renderList=()=>{const list=$('[data-list]'),items=listItems();$('[data-list-title]').textContent={orders:'Orders',deposits:'Deposits',wallets:'Balance'}[state.tab];$('[data-order-filters]').hidden=state.tab!=='orders';if(state.tab==='wallets'){list.innerHTML=`<div class="ps-wallet-grid">${items.length?items.map(w=>`<article class="ps-wallet"><span><strong>Available balance</strong><small>${esc(w.partner_code)} · updated ${esc(relative(w.updated_at))}</small></span><strong>${esc(money(w.balance))}</strong></article>`).join(''):'<div class="ps-empty">No balance record found.</div>'}</div>`;return}if(!items.length){list.innerHTML='<div class="ps-empty">No records match this view.</div>';return}list.innerHTML=items.map(x=>{if(state.tab==='orders'){const[k,label]=orderStatus(x);return`<button type="button" class="ps-row ${state.selectedId===x.id?'is-active':''}" data-record="${esc(x.id)}"><span class="ps-row-icon is-${k}">${svg(k==='store'?'truck':k==='done'?'check':'box')}</span><span class="ps-row-copy"><strong>${esc(x.id)}</strong><span>${esc(productText(x))}</span><small>${units(x)} units · ${esc(relative(x.submitted_at))}</small></span><span class="ps-row-meta"><strong>${esc(money(x.total))}</strong><i class="ps-status is-${k}">${esc(label)}</i></span><svg viewBox="0 0 24 24"><path d="m9 18 6-6-6-6"/></svg></button>`}const[k,label]=depositStatus(x);return`<button type="button" class="ps-row ${String(state.selectedId)===String(x.id)?'is-active':''}" data-record="${x.id}"><span class="ps-row-icon is-${k}">${svg(k==='done'?'check':'wallet')}</span><span class="ps-row-copy"><strong>Deposit #${x.id}</strong><span>${esc(x.proof_name)}</span><small>Submitted ${esc(relative(x.submitted_at))}</small></span><span class="ps-row-meta"><strong>${esc(money(x.approved_amount??x.requested_amount))}</strong><i class="ps-status is-${k}">${esc(label)}</i></span><svg viewBox="0 0 24 24"><path d="m9 18 6-6-6-6"/></svg></button>`}).join('')};
const copyRow=(label,value,icon)=>`<div class="ps-contact-row">${svg(icon)}<span><small>${esc(label)}</small><strong>${esc(value)}</strong></span><button type="button" data-copy="${esc(value)}" title="Copy ${esc(label)}">${svg('copy')}</button></div>`;const events=e=>(e||[]).map((v,i,a)=>`<li class="${i<a.length?'is-done':''}"><i>${svg('check')}</i><span><strong>${esc(v.title)}</strong><small>${esc(dt(v.created_at))} · ${esc(v.actor)}${v.detail?` · ${esc(v.detail)}`:''}</small></span></li>`).join('');
const renderOrder=o=>{const[k,label]=orderStatus(o);const timeline=(o.events||[]);$('[data-detail]').innerHTML=`<header class="ps-detail-head"><div><small>Stock order</small><h3>${esc(o.id)}</h3><p>${esc(o.partner_name)} · submitted ${esc(dt(o.submitted_at))}</p></div><span class="ps-status is-${k}">${esc(label)}</span></header><div class="ps-detail-content"><section class="ps-detail-section"><header><h4>Products</h4><span>${units(o)} units · ${(Number(o.shipping_weight_grams||0)/1000).toLocaleString('en',{maximumFractionDigits:2})} kg est.</span></header><div class="ps-product-card">${(o.items||[]).map(i=>`<div class="ps-product"><span><strong>${esc(i.sku_label||i.product||i.sku_code)}</strong><small>${esc(i.sku_code)} · ${Number(i.quantity||0)} × ${esc(money(i.unit_revenue||i.partner_unit_price))}</small></span><strong>${esc(money(i.line_revenue||Number(i.unit_revenue||0)*Number(i.quantity||0)))}</strong></div>`).join('')}<div class="ps-total"><span>Paid from balance</span><strong>${esc(money(o.total))}</strong></div></div></section><section class="ps-detail-section"><header><h4>Copy-ready shipping</h4><span>Click any copy icon</span></header><div class="ps-contact-card">${copyRow('Full name',o.recipient_name,'box')}${copyRow('Email',o.recipient_email,'mail')}${copyRow('Phone',o.recipient_phone,'phone')}${copyRow('Full address',o.recipient_address,'pin')}${copyRow('Estimated weight',`${(Number(o.shipping_weight_grams||0)/1000).toLocaleString('en',{maximumFractionDigits:2})} kg`,'scale')}</div></section><section class="ps-detail-section"><header><h4>Status timeline</h4></header><ol class="ps-timeline">${events(timeline)||`<li><i>${svg('clock')}</i><span><strong>Order submitted</strong><small>${esc(dt(o.submitted_at))}</small></span></li>`}</ol></section>${o.label?`<section class="ps-detail-section"><header><h4>Stored shipping label</h4></header><a class="ps-upload" href="${esc(o.label.url)}" target="_blank" rel="noopener">${svg('file')}<span><strong>${esc(o.label.name)}</strong><small>${esc(dt(o.label.created_at))} · Open PDF</small></span>${svg('external')}</a></section>`:''}${k==='attention'?`<section class="ps-action-card"><div><span>${svg('truck')}</span><div><strong>Confirm shipment is arranged</strong><small>Upload the final PDF label to release this order to Store Ops.</small></div></div><form data-label-form><label class="ps-upload"><input type="file" name="label" accept=".pdf,application/pdf" required data-label-input>${svg('upload')}<span><strong data-label-name>Choose shipping label PDF</strong><small>Maximum 10 MB</small></span></label><p class="admin-form-error" data-form-error hidden></p><div class="ps-action-row"><button type="submit" class="ps-primary">Upload label · Send to Store Ops</button></div></form></section>`:''}</div>`};
const renderDeposit=d=>{const[k,label]=depositStatus(d),resolved=['approved','rejected'].includes(d.status);$('[data-detail]').innerHTML=`<header class="ps-detail-head"><div><small>Balance request</small><h3>Deposit #${d.id}</h3><p>${esc(d.partner_name)} · submitted ${esc(dt(d.submitted_at))}</p></div><span class="ps-status is-${k}">${esc(label)}</span></header><div class="ps-detail-content"><section class="ps-detail-section"><header><h4>Payment proof</h4><span>${esc(d.proof_name)} · ${(Number(d.proof_size||0)/1048576).toFixed(1)} MB</span></header><div class="ps-proof">${d.proof_mime==='application/pdf'?`<object data="${esc(d.proof_url)}" type="application/pdf"><a href="${esc(d.proof_url)}" target="_blank">Open PDF proof</a></object>`:`<img src="${esc(d.proof_url)}" alt="Payment proof">`}</div></section><section class="ps-detail-section"><header><h4>Review and correction</h4><span>Original request ${esc(money(d.requested_amount))}</span></header><form class="ps-review-card" data-deposit-form><label>Amount to credit<input type="number" name="amount" min="1" max="1000000000000" step="0.01" value="${Number(d.approved_amount??d.requested_amount)}" ${resolved?'disabled':''}></label><label>Review note<textarea name="note" maxlength="1000" placeholder="Document any correction or finding" ${resolved?'disabled':''}>${esc(d.review_note)}</textarea></label><p class="admin-form-error" data-form-error hidden></p>${resolved?`<div class="ps-total"><span>Final amount</span><strong>${esc(money(d.approved_amount??d.requested_amount))}</strong></div>`:`<div class="ps-action-row"><button type="button" class="ps-primary" data-deposit-action="approve_deposit">Approve balance</button><button type="button" class="ps-secondary" data-deposit-action="investigate_deposit">Save investigation</button><button type="button" class="ps-danger" data-deposit-action="reject_deposit">Reject</button></div>`}</form></section><section class="ps-detail-section"><header><h4>Audit timeline</h4></header><ol class="ps-timeline">${events(d.events)}</ol></section></div>`};
const renderDetail=()=>{if(state.tab==='wallets'){$('[data-detail]').innerHTML='<div class="ps-detail-empty">'+svg('wallet')+'<strong>Balance overview</strong><p>Choose Orders or Balance requests to inspect their files and timelines.</p></div>';return}const arr=state.tab==='orders'?state.data.orders:state.data.deposits;const r=arr.find(x=>String(x.id)===String(state.selectedId));if(!r){$('[data-detail]').innerHTML='<div class="ps-detail-empty">'+svg(state.tab==='orders'?'box':'wallet')+'<strong>Select a record</strong><p>Its products, contact details, files, and timeline will appear here.</p></div>';return}state.tab==='orders'?renderOrder(r):renderDeposit(r)};
const render=()=>{const s=state.data.summary||{},identity=state.data.partner||state.data.wallets[0]||state.data.orders[0]||state.data.deposits[0]||{},name=identity.name||identity.partner_name||'Partner';$('[data-partner-top-name]').textContent=name;$('[data-partner-heading]').textContent=`${name} · orders & balance`;$('[data-partner-code-label]').textContent=identity.code||identity.partner_code||state.partner;document.title=`${name} · Partner Activity`;$('[data-metric-deposits]').textContent=s.pending_deposits||0;$('[data-metric-orders]').textContent=s.awaiting_orders||0;$('[data-metric-store]').textContent=s.active_store_ops||0;$('[data-metric-balance]').textContent=money(s.total_balance||0);$('[data-tab-order-count]').textContent=s.awaiting_orders||0;$('[data-tab-deposit-count]').textContent=s.pending_deposits||0;$$('[data-tab]').forEach(b=>b.classList.toggle('is-active',b.dataset.tab===state.tab));renderList();renderDetail()};
const load=async(keep=true)=>{try{const p=await req();state.data=p.data||state.data;if(!keep||!state.selectedId){const arr=state.tab==='orders'?state.data.orders:state.data.deposits;state.selectedId=arr[0]?.id||''}render()}catch(e){$('[data-list]').innerHTML=`<div class="ps-empty">${esc(e.message)}</div>`}};
root.addEventListener('click',async e=>{const b=e.target.closest('button,a');if(!b)return;if(b.dataset.tab){state.tab=b.dataset.tab;state.selectedId='';state.filter='all';$$('[data-filter]').forEach(x=>x.classList.toggle('is-active',x.dataset.filter==='all'));render();if(state.tab!=='wallets'){const arr=state.tab==='orders'?state.data.orders:state.data.deposits;state.selectedId=arr[0]?.id||'';render()}}if(b.dataset.filter){state.filter=b.dataset.filter;$$('[data-filter]').forEach(x=>x.classList.toggle('is-active',x===b));renderList()}if(b.dataset.record){state.selectedId=b.dataset.record;renderList();renderDetail()}if(b.dataset.copy){try{await navigator.clipboard.writeText(b.dataset.copy);toast('Copied to clipboard')}catch{toast('Copy unavailable in this browser')}}if(b.dataset.depositAction){const form=b.closest('[data-deposit-form]'),note=form.elements.note.value;if(b.dataset.depositAction==='reject_deposit'&&!note.trim()){$('[data-form-error]',form).hidden=false;$('[data-form-error]',form).textContent='Add a reason before rejecting.';return}$$('button',form).forEach(x=>x.disabled=true);try{const p=await req({method:'POST',body:JSON.stringify({action:b.dataset.depositAction,deposit_id:Number(state.selectedId),amount:form.elements.amount.value,note,partner_code:state.partner})});state.data=p.data;render();toast(b.dataset.depositAction==='approve_deposit'?'Balance approved':b.dataset.depositAction==='reject_deposit'?'Deposit rejected':'Investigation saved')}catch(x){$$('button',form).forEach(y=>y.disabled=false);$('[data-form-error]',form).hidden=false;$('[data-form-error]',form).textContent=x.message}}});
root.addEventListener('change',e=>{if(e.target.matches('[data-label-input]'))$('[data-label-name]',e.target.closest('form')).textContent=e.target.files?.[0]?.name||'Choose shipping label PDF'});root.addEventListener('submit',async e=>{const form=e.target;if(!form.matches('[data-label-form]'))return;e.preventDefault();const fd=new FormData(form);fd.set('action','arrange_shipment');fd.set('order_id',String(state.selectedId));fd.set('partner_code',state.partner);const submit=$('button[type=submit]',form);submit.disabled=true;submit.textContent='Uploading securely…';try{const p=await req({method:'POST',body:fd});state.data=p.data;render();toast('Shipment arranged · order sent to Store Ops')}catch(x){submit.disabled=false;submit.textContent='Upload label · Send to Store Ops';$('[data-form-error]',form).hidden=false;$('[data-form-error]',form).textContent=x.message}});$('[data-search]').addEventListener('input',e=>{state.search=e.target.value;renderList()});load(true);});
